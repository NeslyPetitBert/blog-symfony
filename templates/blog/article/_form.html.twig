{{ form_start(form) }}
    {{ form_widget(form) }}
    {# {{ form_row(form.title) }}
    {{ form_row(form.content) }}
    {{ form_row(form.image) }} #}
    <button class="btn btn-primary">{{ button_label|default('Créer') }}</button>
{{ form_end(form) }}
